# Restart API Server

1. Go to the terminal where `python api_server.py` is running
2. Press `Ctrl+C` to stop it
3. Run again: `python api_server.py`
4. Try clicking a location on the map that gave an error before
5. Check the terminal - you should now see detailed error messages

The error will show exactly what's failing!
